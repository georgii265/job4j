package ru.job4j.stragery;

public interface Shape {
    /**
     *  В интерфейсе Shape объявить метод String draw().
     *  Метод draw() должен возвращать фигуру в виде строки.
     *  Реализовать интерфейс Shape для квадрата (Square), треугольника (Triangle).
     */
    String draw();


}
