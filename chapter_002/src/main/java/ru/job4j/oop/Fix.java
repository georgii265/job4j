package ru.job4j.oop;

/**
 * 1. Создайте класс Fix. Создайте в нем метод main. Внутри метода main создайте объект класса Fix.
 * 2. Загрузите код в репозиторий. Оставьте ссылку на коммит.
 * --- В этом уроке мы познакомимся с конструктором по умолчанию.
 * --- В каждом классе по умолчанию уже есть конструктор.
 * --- Этот конструктор создается не явно, то есть его создает сама Java.
 */
public class Fix {
    public static void main(String[] args) {
        Fix fix = new Fix();
    }
}
